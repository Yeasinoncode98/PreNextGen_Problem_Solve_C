
#include <stdio.h>

int main()
{
    int n1, n2;

    printf("Enter a first number: ");
    scanf("%d", &n1);

    printf("Enter a second number: ");
    scanf("%d", &n2);

    if (n 1 == n2)
    {
        printf("It is a equal number\n", n1);
    }

    else if (n1 != n2)
    {
        printf("It is not an equal number\n", n2);
    }

    return 0;
}